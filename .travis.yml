language: python
python:
- '2.7'
install:
- pip install -r requirements.txt
script:
- python tests.py
deploy:
  provider: heroku
  apikey: $HEROKU_API_KEY
  app: htmltopdfapi
  on:
    repo: mariusavram91/htmltopdf

